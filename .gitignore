# Generated artefacts (HTML site is rebuilt locally / by CI)
index.html
daily_digest.md
data_sources/registry.json
data_sources/sync_log.json
data_sources/last_run.json
data_sources/validation_report.json
logs/
__pycache__/
*.pyc

# NOTE: intel/ and catalog/ ARE committed — they are the public threat-intel
# and catalog exports that consumers (SIEMs, scripts, TIPs) pull from raw URLs
# on GitHub. They get refreshed each time generate.py runs.

# Local-only files
*.bak_*
inspect_*.py
_check_*.py
_audit_*.py
_migrate_to_yaml.py
