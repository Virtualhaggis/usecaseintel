{
  "schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "resources": [
    {
      "type": "Microsoft.OperationalInsights/workspaces/providers/alertRules",
      "apiVersion": "2022-12-01-preview",
      "name": "[concat(parameters('workspaceName'), '/Microsoft.SecurityInsights/UC_BEACONING')]",
      "kind": "Scheduled",
      "properties": {
        "displayName": "Beaconing \u2014 periodic outbound to small set of destinations",
        "description": "C2 channel detection via inter-beacon-time stddev / fan-out to single dest.",
        "severity": "High",
        "enabled": false,
        "query": "ImNetworkSession\n| where TimeGenerated > ago(2h)\n| where DvcAction != \"Block\"\n| where ipv4_is_private(DstIpAddr) == false\n| summarize ConnCount = count(),\n            DistinctMinutes = dcount(bin(TimeGenerated, 1m))\n            by DvcHostname, DstIpAddr, DstPortNumber\n| where ConnCount > 50 and DistinctMinutes > 30   // sustained, not bursty\n| order by ConnCount desc\n",
        "queryFrequency": "PT1H",
        "queryPeriod": "PT24H",
        "triggerOperator": "GreaterThan",
        "triggerThreshold": 0,
        "tactics": [],
        "techniques": [
          "T1071.001",
          "T1071.004"
        ],
        "alertRuleTemplateName": "UC_BEACONING",
        "customDetails": {
          "tier": "alerting",
          "fp_rate_estimate": "medium",
          "source_url": "https://github.com/Virtualhaggis/usecaseintel/blob/main/use_cases/c2/UC_BEACONING.yml"
        }
      }
    }
  ],
  "parameters": {
    "workspaceName": {
      "type": "string"
    }
  }
}