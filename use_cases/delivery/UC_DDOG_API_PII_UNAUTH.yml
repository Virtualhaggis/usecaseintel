id: UC_DDOG_API_PII_UNAUTH
title: Unauthenticated route returns sensitive PII
kill_chain: delivery
confidence: High
tier: alerting
fp_rate_estimate: low
implementations:
- datadog
mitre_attack:
- id: T1526
  name: Cloud Service Discovery
- id: T1538
  name: Cloud Service Dashboard
description: |-
  API endpoint without authentication returning PII (email, phone, SSN-shape, card-shape) in its response body — direct data-exposure path. Datadog default 'Unauthenticated route returns sensitive PII'.
datadog_query: |-
  source:api_findings @category:data_exposure
  @finding.type:(pii_in_response OR sensitive_data_unauthenticated)
