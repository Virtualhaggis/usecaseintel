id: UC_DDOG_K8S_RBAC_BINDING_CREATED
title: Kubernetes RBAC role binding created
kill_chain: install
confidence: High
tier: alerting
fp_rate_estimate: low
implementations:
- datadog
mitre_attack:
- id: T1098
  name: Account Manipulation
description: |-
  RBAC binding added — privilege drift inside the cluster. Datadog default 'Kubernetes RBAC role binding created'.
datadog_query: |-
  source:kubernetes.audit @verb:create
  @objectRef.resource:(rolebindings OR clusterrolebindings)
