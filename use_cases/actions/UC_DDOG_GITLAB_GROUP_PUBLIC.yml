id: UC_DDOG_GITLAB_GROUP_PUBLIC
title: GitLab group visibility changed to public
kill_chain: actions
confidence: High
tier: alerting
fp_rate_estimate: low
implementations:
- datadog
mitre_attack:
- id: T1538
  name: Cloud Service Dashboard
description: |-
  Private GitLab group flipped to public — exposure. Datadog default 'GitLab group visibility changed to public'.
datadog_query: |-
  source:gitlab @evt.name:group.update @target.visibility:public
