feedparser>=6.0
requests>=2.28
pyyaml>=6.0
# Optional — only required if ANTHROPIC_API_KEY is set to enable
# LLM-driven bespoke UC generation. Pipeline runs without it.
anthropic>=0.30; python_version >= "3.9"
